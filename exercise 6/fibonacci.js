// 1. Create a function that calculates the nth fibonacci number based on the
// definition.
// fib(0) is 0.
// fib(1) is 1.
// fib(n) is fib(n-1) + fib(n-2) for n>=2.

// 2. Create a function that accepts an array and increments each value in the
// array by 1. You can assume the input only contains numbers. Then print the
// return value of the array and the original array to the console. Make sure the 
// original array is not changed.

// 3. Create a function that generates an HTML unordered list from an array and
// returns the generated unordered list HTML markup as a string.

// 4. Create a function that generates an HTML table from a 3x3 array and
// returns the generated table HTML markup as a string.

